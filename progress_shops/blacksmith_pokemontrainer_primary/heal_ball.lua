function ADVR.onLoad()
    progress.name = "Heal ball"
    progress.desc = "Putting your pokemon back into the ball fully restores their health"
	progress.price = 35
	progress.currency = "gold"
    
end

function ADVR.ProgressEvents.onBuy()
end

