function ADVR.onLoad()
    progress.name = "Level Ball"
    progress.desc = "Increases catch chance if the last mon you caught was from a previous floor than the one you are attempting to catch."
	progress.price = 3
	progress.currency = "beastblood"
    
end

function ADVR.ProgressEvents.onBuy()
end

