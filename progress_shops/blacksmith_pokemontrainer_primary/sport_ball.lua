function ADVR.onLoad()
    progress.name = "Sport Ball"
    progress.desc = "You have a higher base chance to catch pokemon in arena mode"
	progress.price = 10
	progress.currency = "gold"
    
end

function ADVR.ProgressEvents.onBuy()
end

