function ADVR.onLoad()
    progress.name = "Quick Ball"
    progress.desc = "Your first use of the pokeball will never fail"
	progress.price = 5
	progress.currency = "beastblood"
    
end

function ADVR.ProgressEvents.onBuy()
end

